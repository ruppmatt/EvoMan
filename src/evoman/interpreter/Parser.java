package evoman.interpreter;


public class Parser {

	// Read block
	// Verify indent

	// For each line
	// If indent, start new block
	// If keyword @import or @include or @importall
	// If keyword def, must be top block
	// If typename (in global or local dictionary), expect name

	// If end of block
	// Try to instantiate block using inside block

}
